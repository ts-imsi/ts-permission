<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trasen.permission.dao.TwfDictMapper">
    <select id="getTwfDictForType" parameterType="int" resultType="com.trasen.permission.model.TwfDict">
        select * from twf_dict where type=#{type} and is_vaild=1
    </select>
</mapper>